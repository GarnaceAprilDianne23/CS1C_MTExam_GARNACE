// colorPreferences.js

let favoriteColors = [];

while (favoriteColors.length < 3) {
    let color = prompt(`Enter color #${favoriteColors.length + 1}:`);
    favoriteColors.push(color);
    console.log("Current Color List:", favoriteColors);
}
